<resources>
    <!-- Base application theme. The real theming lives in Compose (Temp0Theme); this XML
         theme only needs to set a dark window background so there's no white flash before
         the first Compose frame draws, and to disable the action bar. -->
    <style name="Theme.Temp0" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/app_bg</item>
        <item name="android:statusBarColor">@color/app_bg</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
